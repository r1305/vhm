# cPanel — Libro de Reclamaciones (VHM)

- **Startup file:** `app.js` (ya compatible con Passenger)
- **1 app Node** por dominio
- Copiar `.env.example` → `.env` con DB y `JWT_SECRET`
- No usar PM2 cluster en hosting compartido

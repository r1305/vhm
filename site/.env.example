# ============================================================
# VHM - Libro de Reclamaciones Virtual
# ============================================================
# COPIA este archivo como .env y rellena los valores.
# NUNCA subas .env al repositorio (ya está en .gitignore).
# ============================================================

NODE_ENV=production
PORT=3000
APP_MOUNT_PATH=/site
# En cPanel con URL temporal usa: APP_MOUNT_PATH=/~usuario/site

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_POOL_MAX=3
DB_POOL_IDLE_MAX=1

# Cron interno La Tribu (miércoles 12:00). Dejar en 0 en cPanel; usa cron externo si lo necesitas.
TRIBU_CRON_ENABLED=0

# Renovación automática de suscripciones La Tribu (Orders API + tarjeta guardada).
# En cPanel: cron HTTP diario a GET /api/tribu-pagos/cron-renovaciones?token=...
# Genera el secret: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
TRIBU_RENOVACION_CRON_SECRET=
# Cron interno cada hora (0 en hosting compartido; preferir cron HTTP externo)
TRIBU_RENOVACION_CRON_ENABLED=0

# Máximo de procesos Node bajo Passenger (hosting compartido: 2)
PASSENGER_MAX_POOL_SIZE=2

# JWT - Genera un secret aleatorio: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NUNCA uses un valor predecible como "mi_secreto_2026"
JWT_SECRET=
CORS_ORIGIN=https://tudominio.com

# URL pública del sitio (incluye APP_MOUNT_PATH si aplica), para webhooks de Culqi
# Ejemplo: https://vhm.com.pe/site
SITE_URL=

# Secreto opcional para validar firma de webhooks Culqi (CulqiPanel → Webhooks)
CULQI_WEBHOOK_SECRET=

# Chat IA "Clara" (Opción B). Si se define OPENAI_API_KEY, el widget muestra
# un chat embebido en la página. Si se deja vacío, el widget abre el GPT de
# Clara en una ventana emergente (Opción A). NUNCA subir la key real al repo.
OPENAI_API_KEY=
CLARA_MODEL=gpt-4o-mini
# CLARA_SYSTEM_PROMPT=Instrucciones/personalidad de Clara (opcional)

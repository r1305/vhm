RewriteEngine On
RewriteBase /crm/

# Archivos y directorios reales pasan directo
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás lo maneja Passenger/Node
RewriteRule ^ - [L]
